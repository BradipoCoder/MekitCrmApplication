{% if resource_granted('mekit_event_view') %}
	{% import 'OroUIBundle::macros.html.twig' as UI %}

	{% set widgetContent %}
	{{ oro_widget_render({
		'widgetType': 'block',
		'url': path('mekit_crm_widget_related', {
			datagrid_name: datagrid_name,
			id: entity.id
		}),
		'cssClass': 'list-widget mekit_related_tasks',
	}) }}
	{% endset %}

	{{ UI.scrollSubblock(null, [widgetContent]) }}

{% endif %}
