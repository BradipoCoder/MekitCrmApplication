{% if resource_granted('mekit_meeting_create') %}
	{% import 'OroUIBundle::macros.html.twig' as UI %}

	{% set dialogTitle = 'mekit.meeting.placeholders.register_meeting'|trans %}
	{% set gridName = mekit_related_datagrid_name("meeting", entity) %}

	{{ UI.clientButton({
	    'dataUrl': path(
	        'mekit_meeting_create',
			oro_action_params(entity, 'assign')
		),
	    'aCss': 'no-hash',
	    'iCss': 'icon-group',
	    'dataId': entity.id,
	    'label' : dialogTitle,
		'title' : dialogTitle,
	    'widget' : {
	        'type' : 'dialog',
	        'multiple' : false,
			'reload-grid-name' : gridName,
	        'options' : {
	            'alias': 'task-dialog',
	            'dialogOptions' : {
	                'title' : dialogTitle,
	                'allowMaximize': false,
	                'allowMinimize': false,
	                'width': 925
	            }
	        }
	    }
	}) }}
{% endif %}
