{# List of users assigned to this record #}

{% set assignees = record.getValue('users') %}
{#{% set assignees = record.get('users') %}#}


{#{% for assignee in assignees %}#}
	{#{{ assignee.firstName }}#}
{#{% endfor %}#}

{#{{ dump(value) }}#}
{#{{ dump(assignees) }}#}

{{ dump(record) }}
{{ dump(assignees) }}