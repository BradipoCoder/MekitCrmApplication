
{% set relatedTasks %}
	{% placeholder mekit_related_tasks with {entity: entity, datagrid_name: "projects-related-tasks"} %}
{% endset %}

{#{% if relatedTasks is defined and relatedTasks is not empty %}#}
	{% set dataBlocks = dataBlocks|merge([{
	'title': 'mekit.project.tasks.label'|trans,
	'subblocks': [{'data': [relatedTasks], 'spanClass': 'empty'}]
	}]) %}
{#{% endif %}#}